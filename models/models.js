export default class ProductsModel {
    constructor (id, name, price, thumbnail) {
        return {
            id: id,
            name: name,
            price: price,
            thumbnail, thumbnail
        }
    }
}